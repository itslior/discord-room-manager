version: '3.8'

services:
  bot:
    build: .
    container_name: discord-room-manager
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
